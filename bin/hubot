#!/bin/sh

set -e

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

#case $1 in
#  "start" | "stop" | "restart" )
#    forever $1 \
#    -c coffee node_modules/.bin/hubot -a slack -n neo
#  ;;
#  * ) echo "usage: neobot start|stop|retart" ;;
#esac

exec node_modules/.bin/hubot --name "neo" "$@"
